# M2 execution history

> Historical record preserved from main cc93ae9 (September 8, 2026). Status statements and authorizations describe their original tasks. Use the [current roadmap](../ROADMAP.md) for new work.

## 14. M2 execution record

This ordered table records how M2 was delivered. It no longer routes new work; Section 15 is the current execution queue. The eligibility, ownership, and transition rules in `AGENTS.md` and the state meanings recorded in Section 12 still apply. Every collector slice used only synthetic repositories and remained offline, bounded, non-executing, and inside an explicitly authorized root.

| Order | Slice | State | Observable outcome | Prerequisites and traceability |
|---:|---|---|---|---|
| 1 | `M2-S01` — Authorized-root configuration boundary | `Complete` | An accepted ADR and implementation-internal configuration boundary define developer-selected repositories, canonical authorized roots, platform-aware path identity, limits, and content-free failures without reading repository contents. | M1 exit; M2 authorized-root deliverable; `FMU-FR-001`, `FMU-FR-026`; `FMU-NFR-009`, `FMU-NFR-013`; ADR-0018. |
| 2 | `M2-S02` — Bounded filesystem metadata collectors | `Complete` | Deterministic collectors read only selected documents, manifests, and supported source structure under canonical authorized roots while rejecting traversal, symlink/junction escape, binary data, and size/depth/count/time exhaustion without executing repository content. | M2-S01; `FMU-FR-002`; `FMU-NFR-011`, `FMU-NFR-013`; path, binary, malicious-text, and resource-limit security cases; ADR-0019. |
| 3 | `M2-S03` — Bounded Git metadata collector | `Complete` | A sanitized, non-executing Git boundary extracts only bounded metadata from selected repositories and rejects hooks, filters, external commands, unsafe configuration, and content-bearing diagnostics. | M2-S02; `FMU-FR-002`, `FMU-FR-026`; `FMU-NFR-004`, `FMU-NFR-011`, `FMU-NFR-013`; command-injection security cases; ADR-0020. |
| 4 | `M2-S04` — Developer identity and authorship assessment | `Complete` | Explicit identity configuration and deterministic attribution distinguish known, shared, coauthored, bot, squash, pair-work, and unknown authorship without allowing unknown authorship alone to establish demonstrated depth. | M2-S03; `FMU-FR-006`, `FMU-FR-018`; `FMU-E-008`; `FMU-NFR-006`; ADR-0021. |
| 5 | `M2-S05` — Evidence source-risk classification | `Complete` | Collected evidence records deterministically expose and down-rank fork, template, generated, vendored, tutorial, duplicated, and uncertain source contributions while preserving their limitations. | M2-S04; `FMU-FR-005`, `FMU-FR-006`, `FMU-FR-019`; `FMU-E-007`; `FMU-NFR-006`; ADR-0022. |
| 6 | `M2-S06` — Atomic versioned local profile store | `Complete` | The internal profile store validates versions, writes atomically, preserves the prior valid state on interruption, recovers deterministically, and runs explicit forward migrations over synthetic data. | M2-S05; `FMU-FR-004`, `FMU-FR-009`; `FMU-NFR-009`, `FMU-NFR-014`; persistence and interrupted-write cases from `FMU-E-011`; ADR-0023. |
| 7 | `M2-S07` — Deterministic claim derivation and invalidation | `Complete` | Evidence produces humble, traceable Claims with provenance, scope, freshness, fingerprints, limitations, and invalidation; no high-confidence automated Claim lacks attributable evidence. | M2-S06; `FMU-FR-004` through `FMU-FR-006`, `FMU-FR-020`; `FMU-NFR-006`; ADR-0008; ADR-0024. |
| 8 | `M2-S08` — Source-backed Demand Profile producer | `Complete` | Bounded current-project/task inputs produce a schema-valid Demand Profile and ask at most one high-information question only when ambiguity materially changes behavior or risk. | M2-S07; `FMU-FR-003`, `FMU-FR-021`; `FMU-E-009`, `FMU-E-010`; ADR-0010; ADR-0025. |
| 9 | `M2-S09` — Multi-repository incremental orchestration | `Complete` | Multiple selected local repositories refresh within explicit budgets; unchanged fingerprints avoid full rescans and cache origin, fresh, stale, partial, and invalid states remain observable. | M2-S08; `FMU-FR-017`, `FMU-FR-020`; `FMU-NFR-003`, `FMU-NFR-013`; ADR-0026. |
| 10 | `M2-S10` — Owner inspection and correction workflow | `Complete` | A no-LLM owner interface inspects, edits, corrects, disputes, and rejects Claims; correction precedence remains traceable and success is reported only after validated persistence. | M2-S09; `FMU-FR-013`, `FMU-FR-014`; `FMU-E-005`, `FMU-E-011`; ADR-0027. |
| 11 | `M2-S11` — Owner import, export, and deletion | `Complete` | Explicit owner operations validate imports, create bounded Portable Profile Exports without credentials/raw private source, and verifiably delete local profile data and caches. | M2-S10; `FMU-FR-015`, `FMU-FR-026`; `FMU-E-015`; ADR-0009/0028. |
| 12 | `M2-S12` — Bounded evidence lookup and secret-safe doctor | `Complete` | The owner can request bounded capability evidence and diagnose installation, schema, adapter, cache, and context-size state without exposing source, secrets, personal paths, or canaries. | M2-S11; `FMU-FR-010`, `FMU-FR-022`, `FMU-FR-023`, `FMU-FR-026`; `FMU-E-015`. |
| 13 | `M2-S13` — End-to-end local Community workflow | `Complete` | A clean local workflow selects authorized local repositories, collects evidence, persists/corrects a profile, compiles task context, serves the compatible consumer, diagnoses state, exports, and deletes without an account, network, dedicated LLM API, or proprietary service; verification uses only synthetic repositories. | M2-S12; `FMU-FR-016`, `FMU-FR-023`; `FMU-NFR-004`, `FMU-NFR-011`, `FMU-NFR-016`; M2 local-utility exit gate. |
| 14 | `M2-S14` — Evidence-quality experiment brief | `Complete` | The owner-delegated research decision freezes 48 cases, two languages, ten invariance pairs and zero-error conformance thresholds in a hashed protocol before measurement, with AI-assisted-development interpretation limits. | M2-S13 and the M2 measurement exit gate; ADR-0031 and the frozen quality protocol. |
| 15 | `M2-S15` — Limited-taxonomy quality measurement | `Complete` | The frozen sample runs reproducibly and reports false-`demonstrated`, attribution, unknown, correction, and rejection outcomes against the accepted thresholds without tuning the brief after results. | M2-S14; M2 measurement exit gate; Project Specification profile-quality metrics. |
| 16 | `M2-S16` — M2 exit audit | `Complete` | An integrated audit runs every required behavioral, persistence, recovery, cache, owner-control, security, clean-install, and local-utility gate and either closes M2 or names a blocker. | M2-S01 through M2-S15; full M2 exit gate, required evaluations, and cross-milestone quality gates. The audit is recorded in [`docs/audits/M2_EXIT_AUDIT.md`](../audits/M2_EXIT_AUDIT.md). |

### M2-S01 authorized-root configuration boundary

[ADR-0018](../adr/0018-authorized-local-repository-configuration.md) places the first M2 source boundary inside the Community Provider while keeping Protocol, Core, and the Profile Provider exchanges path-free. The implementation accepts at most 32 KiB of exact internal JSON `0.1.0`, requires explicit absolute roots plus opaque root/repository identifiers and safe root-relative repository paths, and fixes hard ceilings for roots, repositories, files, per-file/total bytes, depth, duration, and concurrency before a collector exists.

A narrow injected directory port resolves only physical path metadata. Its Node implementation uses `realpath` and `stat`, requires directories, normalizes platform-specific identity, treats Windows identity as case-insensitive, rejects Windows device namespaces, and denies canonical repository escape and duplicate physical aliases. Resolution is all-or-nothing: invalid shape/version/limits, missing or non-directory paths, and failed authorization return only a fixed category and retryability, never an input path, identifier, native error, stack, or partial authority. Successful private results are detached, sorted, and deeply immutable.

Unit coverage exercises exact byte/array/string/numeric bounds, closed fields, identifiers, safe relative syntax, root references, Windows/POSIX identity, sibling-prefix and canonical escape, logical/physical duplicates, invalid canonicalizer output, content-free failures, deterministic ordering, and immutability. Integration coverage uses temporary synthetic directories, proves metadata-only resolution, rejects a real junction/symlink escape, and covers missing/non-directory selections. Static inspection prohibits enumeration, content-read, process-execution, network, and listener primitives in the boundary. The complete aggregate passed with 125 unit tests, every schema corpus, 15 integration tests, and ten behavioral evaluations; the existing dependency graph and public schemas are unchanged.

This configuration-time result is a private authorization snapshot, not a Source Grant, Sharing Grant, permanent filesystem capability, collector, or public contract. M2-S02 must recanonicalize every path at the moment of use and recheck it against the stored canonical root before any bounded read. There is no repository enumeration/content access, Git execution, Evidence/Claim/Demand production, persistence, owner CLI, MCP operation, new dependency, release, publication, private-source access, or broader platform claim. No `FMU-E-*` applies because consumer behavior does not change.

The proposed M2 `In progress`, M2-S01 `Complete`, and M2-S02 `Ready` transitions become authoritative only after this revision passes required pull-request CI and integrates into protected `main`.

### M2-S02 bounded filesystem metadata collectors

[ADR-0019](../adr/0019-bounded-filesystem-metadata-collector.md) keeps repository reading inside the Community Provider and requires a live authority issued by M2-S01. A narrow injected filesystem port re-canonicalizes roots, repositories, directories, and files at point of use; rechecks containment against both the named root and selected repository; rejects links, special files, replacements, unsafe names, and path escape; and performs deterministic sequential traversal. Fixed version-control, dependency, and Fork Me Up state directories are ignored, while a `.git` marker makes a nested directory a repository boundary without enumerating its contents.

The collector opens only a fixed taxonomy of regular UTF-8 documents, manifests, and source files within exact entry, per-file, total-byte, depth, elapsed-time, and configured repository ceilings. Unsupported files are counted but not opened, archives are not expanded, and no repository code, hook, filter, package script, command, URL, network primitive, or Git operation is invoked. The all-or-nothing private `filesystem-metadata-snapshot` `0.1.0` contains only safe repository-relative paths, byte/line/structure counts, SHA-256 digests, fixed formats/languages, deterministic test markers, and bounded sorted `package.json` dependency/script names. Raw text, headings, snippets, package identity/version/values, script values, absolute paths, and native diagnostics are absent.

Unit coverage exercises live versus forged authority, deterministic sanitized output, immutability, ignored and nested boundaries, unsupported files, point-of-use replacement/escape, binary and invalid UTF-8, malformed JSON, native-error canaries, file growth, and entry/per-file/total-byte/depth/deadline exhaustion. Integration coverage uses only temporary synthetic repositories and real read-only filesystem calls for stable collection, ignored/nested trees, real junction/symlink rejection, binary/malformed/oversized content, and depth/count limits. Static inspection prohibits process, network, listener, write, dynamic-import, URL-following, and archive behavior. The public schemas, Provider/MCP operations, package graph, and lockfile are unchanged; no behavioral `FMU-E-*` applies because no Claim, Demand, DCP, or consumer behavior is produced.

The proposed M2-S02 `Complete` and M2-S03 `Ready` transitions become authoritative only after this revision passes required pull-request CI and integrates into protected `main`.

### M2-S03 bounded Git metadata collector

[ADR-0020](../adr/0020-quarantined-bounded-git-metadata-collector.md) keeps Git collection inside the Community Provider and requires a live M2-S01 authority. The collector reauthorizes the selected root and repository, accepts only a standard contained `.git` directory and object database, resolves bounded symbolic/detached/packed/shallow control data without Git, and fingerprints the bounded object-store structure before and after use. Links, special entries, common-directory indirection, alternates, path escape/replacement, malformed identifiers, and count/byte/depth/deadline exhaustion fail closed.

Git never runs against the repository's own Git directory. Each exact allowlisted `rev-list`, `cat-file`, or `diff-tree` invocation uses `shell: false`, fixed arguments, bounded piped bytes, a minimal environment, and a newly created trusted bare quarantine pointing only at the authorized object database. Repository/system/global configuration, worktree attributes, hooks, filters, aliases, fsmonitor, external diff/textconv, pagers, replacement objects, lazy fetch, prompts, credentials, remotes, and source writes are disabled or structurally excluded. The immutable private `git-metadata-snapshot` `0.1.0` contains only bounded SHA-1/SHA-256 object IDs, parent relationships, UTC timestamps, SHA-256 identity/coauthor digests, safe changed paths, and explicit shallow/truncation state; raw names, emails, messages, refs, URLs, absolute paths, stderr, and native diagnostics are absent.

Unit and integration coverage exercises authentic/forged authority, exact command validation, deterministic 256-commit truncation, fixed failure mapping, hostile and syntactically invalid config, malicious attributes and messages, hooks/external commands, replace refs, merge parents, coauthors, packed and shallow heads, SHA-256 repositories, alternates and linked object stores, unsupported layouts, Git failure, and object/count/byte/depth limits using only temporary synthetic repositories. No dependency, public schema, Protocol/Core/Provider/MCP behavior, Evidence, Claim, identity attribution, source-risk decision, persistence, network, release, or migration is introduced; no `FMU-E-*` applies because consumer behavior remains unchanged.

The proposed M2-S03 `Complete` and M2-S04 `Ready` transitions become authoritative only after this revision passes required pull-request CI and integrates into protected `main`.

### M2-S04 developer identity and authorship assessment

[ADR-0021](../adr/0021-explicit-conservative-git-authorship-assessment.md) keeps identity configuration and pre-Evidence assessment inside the Community Provider. The closed implementation-internal resolver accepts at most 32 KiB of explicit target, developer/shared/bot Git identities, and pair-work/squash commit annotations. Private names/emails are normalized through the exact M2-S03 algorithm and immediately replaced with lowercase SHA-256 digests; resolved configuration is sorted, deeply immutable, role-disjoint, and authentic, with no raw identity retained. No automatic identity, bot-name, or squash inference is used.

The pure assessor revalidates the complete bounded Git snapshot and every annotation reference. It independently records attribution (`attributed`, `shared`, `coauthored`, `bot`, `pair-work`, or `unknown`) and history (`ordinary`, structural `merge`, or explicit `squash`), plus bot/committer/coauthor/shallow/truncation limitations. Direct and recognized collaborative participation map conservatively to the existing Evidence author states and opaque target subject, while shared or unmatched identities map to `unknown` without a subject. Every isolated commit explicitly prohibits standalone demonstrated depth: direct attribution reaches at most medium/practical-use, coauthor/pair/squash reaches at most low/exposure, and shared/bot/unknown reaches low with no personal-depth support.

Unit coverage exercises exact configuration byte/count/shape/version bounds, normalization, duplicate cross-role identities, invalid private identity data, authentic versus forged resolved state, every attribution/history/mapping/ceiling, deterministic order, deep immutability, malformed snapshots, dangling annotations, unsafe paths, canaries, and absence of filesystem/process/network primitives. Integration uses only a temporary synthetic repository and proves that real M2-S03 author/coauthor digests match normalized private aliases across direct, shared, coauthored, bot, explicit pair-work, explicit squash, and unknown cases without returning names, emails, messages, paths, or source content. `FMU-E-008` now proves shared and unknown authorship cannot establish high-confidence personal depth. No public schema, dependency, Evidence/Claim/capability production, source-risk classification, persistence, Provider/MCP/adapter behavior, network, release, or migration is introduced.

The proposed M2-S04 `Complete` and M2-S05 `Ready` transitions become authoritative only after this revision passes required pull-request CI and integrates into protected `main`.

### M2-S05 evidence source-risk classification

[ADR-0022](../adr/0022-conservative-evidence-source-risk-classification.md) adds a pure implementation-internal classification boundary after the M2-S02 through M2-S04 snapshots. A closed 32 KiB configuration accepts only explicit repository-level fork/template/tutorial/uncertain annotations and path-level template/generated/vendored/tutorial/uncertain annotations. Duplicate state is never manually asserted: it is derived only from equal SHA-256 file digests across the complete bounded input.

The classifier revalidates filesystem and Git snapshots, requires authentic configuration and authorship results, and demands exact repository, root, commit-assessment, and annotation-target correspondence before emitting anything. Fixed case-insensitive path segments identify generated, vendored, tutorial, and template locations; fixed filename forms identify common generated artifacts. These indicators are limitations, not claims of provenance. Files without a bounded changed-path association, or without any attributed/coauthored associated commit, become `uncertain`.

Each immutable private record exposes only a safe source-relative reference, digest, source category, bounded commit references, attribution states, authorship limitations, risk flags, and source limitations. Any risk flag reduces support and caps strength at weak. An unflagged record remains merely normal/moderate with `origin-unverified`; it is never labeled original. Every record prohibits standalone demonstrated depth. Unit, real synthetic-repository integration, and `FMU-E-007` evaluation coverage exercise explicit and inferred risk classes, exact same/cross-repository duplicates, shallow/truncated history, missing association, forged inputs, snapshot/configuration mismatch, deterministic immutable output, canary redaction, and absence of filesystem/process/network authority in the pure classifier. Pinned clean-install verification passes the complete aggregate with 148 unit tests, every schema corpus, 26 integration tests, and 12 behavioral evaluations; the point-in-time dependency audit reports zero vulnerabilities. No public Evidence/Claim schema, capability derivation, persistence, dependency, Provider/MCP/adapter behavior, network, release, or migration is introduced.

The proposed M2-S05 `Complete` and M2-S06 `Ready` transitions become authoritative only after this revision passes required pull-request CI and integrates into protected `main`.

### M2-S06 atomic versioned local profile store

[ADR-0023](../adr/0023-generation-addressed-atomic-local-profile-store.md) implements the runtime half of the internal Store boundary accepted in ADR-0009. A closed private configuration canonicalizes one existing store directory and binds it to expected opaque Store and subject identifiers. Every operation rechecks the canonical directory identity. Current and synthetic legacy JSON are byte-bounded, exact-shape parsed, detached, deeply immutable, timestamp ordered, and validated through the existing complete Profile payload contract before they can become candidates.

Committed Store values use immutable generation-addressed filenames. A write first creates a nonce-bearing same-directory temporary exclusively, writes and synchronizes canonical bytes, and rereads the exact validated value. It then atomically creates the never-overwritten final generation through an exclusive hard link, synchronizes the directory where supported, rereads the final value, and only then returns `committed`. The expected generation plus exclusive final filename provide cross-process optimistic concurrency without an abandonable lock. A crash before linking leaves only an inactive temporary; a crash after linking leaves a complete new generation while the prior committed generation remains available.

Load enumerates a fixed bounded filename grammar, rejects redirected/special committed entries, validates every candidate, and selects the highest valid generation. A corrupt higher generation cannot hide the prior valid Store and yields explicit `recovered` state; orphan temporaries are never activated. Successful activation retains the latest rollback generation and best-effort cleanup reports `maintenanceRequired` rather than turning an already committed result into a false failure. A synthetic exact `0.0.0` rehearsal format lacking validation/migration bookkeeping migrates deterministically to a new `0.1.0` generation using an injected timestamp and the same atomic activation path; it is not claimed as released user data.

Unit, real-filesystem integration, and `FMU-E-011` coverage exercise exact versions/shapes/limits, authentic configuration, creation/update/readback, stale and simultaneous writers, injected failure at staging/link/sync/readback/cleanup, old-generation retention, corruption recovery, orphan staging, scan bounds, link rejection, and explicit migration. Pinned clean-install verification passes the complete aggregate with 157 unit tests, every schema corpus, 28 integration tests, and 13 behavioral evaluations; the point-in-time dependency audit reports zero vulnerabilities. No public schema, Claim/Evidence derivation, correction behavior, owner operation, Provider/MCP/adapter behavior, dependency, network service, release, or real-data migration is introduced.

The proposed M2-S06 `Complete` and M2-S07 `Ready` transitions become authoritative only after this revision passes required pull-request CI and integrates into protected `main`.

### M2-S07 deterministic Evidence and Claim derivation

[ADR-0024](../adr/0024-deterministic-evidence-claim-derivation.md) adds a pure implementation-internal Community producer after the authentic M2-S05 source-risk boundary. Source-risk records now carry only their already normalized source language or `null`; the derivation admits only source-file languages as `language.*` capability signals. Documents, manifest/dependency/script names, paths, prose, annotations, and arbitrary taxonomy configuration have no capability authority. An exact `0.1.0` request injects source observation, derivation, and stale-threshold times plus a complete repository-to-project mapping; no wall clock, randomness, filesystem, process, model, or network participates.

Each exact-schema Evidence record preserves safe source/repository/revision references, mapped attributable/coauthored/bot/unknown authorship, local-only visibility, upstream strength and authorship depth/confidence ceilings, fixed bounded limitations, extractor version, a stable opaque ID, and a semantic source fingerprint. Claims group only exact same-project language observations. Attributable/coauthored support is required for `demonstrated`; otherwise the state is `insufficient-evidence`. One, weak, or collaboratively capped observation reaches only exposure, while two distinct direct moderate observations may reach practical use only when both authorship ceilings permit it. Generated Claims are always project-scoped, at most medium confidence, and never `demonstrated-depth`, adjacent, declared, disputed, global, or a score.

Stable Claim IDs and internal fingerprints make refresh comparison explicit. Against an authentic non-regressing prior derivation, missing or changed Evidence reports `source-unavailable` or `fingerprint-changed`; affected Claims report `no-longer-supported`, `evidence-invalidated`, `support-changed`, or `freshness-changed`. Unrelated head movement and collection time are excluded from source fingerprints. The complete project/Evidence/Claim reference graph is validated through the accepted Profile payload boundary before a detached deeply immutable result is returned.

Unit and real-chain integration coverage exercises direct, collaborative, bot, unknown, weak and repeated moderate signals; exact version/shape/timestamp/mapping rules; stable IDs/fingerprints; stale state; evidence-reference and limitation bounds; deterministic order; every invalidation reason; forged current/prior snapshots; backward time; content-free failures; canaries; Store-compatible serialization; and source refresh using only a temporary synthetic repository. Pinned verification passes the complete aggregate with 165 unit tests, every schema corpus, 29 integration tests, and 13 behavioral evaluations; no new behavioral `FMU-E-*` applies because consumer and owner behavior does not change. The point-in-time dependency audit is required before integration. No public schema, dependency, adjacency/declaration/correction behavior, Demand production, cache orchestration, owner operation, Provider/MCP/adapter behavior, network service, release, or migration is introduced.

The proposed M2-S07 `Complete` and M2-S08 `Ready` transitions become authoritative only after this revision passes required pull-request CI and integrates into protected `main`.

### M2-S08 source-backed Demand Profile producer

[ADR-0025](../adr/0025-source-backed-demand-profile-producer.md) adds a pure Community producer from a closed 32 KiB task request and an authentic immutable M2-S02 filesystem snapshot or explicit unavailable metadata. One current repository/project binding and optional exact collected-file selection exclude other sources. Only normalized source languages contribute supporting `language.*` demand; explicit task capabilities take precedence with exact task/project basis. Dependency names, scripts, documents and task prose cannot infer capabilities or personal knowledge. The final union must fit the existing 32-capability Protocol contract, and empty demand remains valid.

Available, partial unsupported-file coverage and unavailable metadata remain distinct, with a deterministic opaque revision for collected metadata. Task prose is replaced with a fixed minimized summary; source paths, repository names, manifests, raw text, profile records and diagnostics are absent. A set of at most four explicit interpretations yields one fixed clarification only when effective capability/relevance or operation risk differs. Equivalent interpretations retain common task input and ask nothing. An authentic pending object accepts an offered choice and returns a Demand Profile without a second question; absent, forged or invalid answers cannot silently resolve the decision. Operation labels never authorize actions.

Pinned Windows Node.js 24.20.0/npm 11.19.0 verification passed an offline lockfile-enforced clean installation and the complete aggregate: 173 unit tests, every schema corpus, 30 integration tests and 15 behavioral evaluations. `FMU-E-009` and `FMU-E-010` now exercise material and non-material ambiguity; real temporary-directory integration composes collection, demand production and Core intersection with canaries and source refresh. A point-in-time dependency audit reports zero vulnerabilities. Independent static review found no concrete defect; this is not a substitute for focused human review and required PR checks. No public schema, dependency, migration, persistence/cache orchestration, owner/consumer workflow, network runtime, release or broader platform claim is introduced.

The proposed M2-S08 `Complete` and M2-S09 `Ready` transitions become authoritative only after this revision passes required pull-request CI and reviewed integration into protected `main`.

### M2-S09 multi-repository incremental orchestration

[ADR-0026](../adr/0026-bounded-incremental-local-refresh.md) adds a private process-local refresh session bound to authentic repository, identity and source-risk configuration plus complete repository/project mappings. A bounded metadata-only probe checks canonical identity, membership and device/inode/mode/size/mtime/ctime stamps across selected source and standard Git metadata. Unchanged repositories within the configured observation age reuse authentic filesystem/Git snapshots with zero source-content reads or Git commands; changed, expired or forced sources are collected again and probed before acceptance. Cache hits retain the original collection time.

Refreshes enforce aggregate enumeration, collection, duration and retained serialized-state budgets, including ignored entries, failed-enumeration reservations and the prior derivation. Negative, regressing, non-finite or throwing clocks invalidate the refresh and clear source cache. Per-repository origin, fingerprint, collected/checked times and fixed fresh/stale/invalid reasons remain explicit. Cache freshness describes usability; Evidence and Claims preserve their separate age semantics. Stale or unavailable peers withhold the complete aggregate payload so missing duplicates cannot inflate support. Complete refreshes rerun global authorship/source-risk assessment and deterministic derivation using the oldest collection time, preserve invalidation history and expose authentic snapshots to the existing Demand producer.

Pinned Windows Node.js 24.20.0/npm 11.19.0 verification passed an offline lockfile-enforced clean installation and the complete aggregate: 193 unit tests, every schema corpus, 32 integration tests and 15 behavioral evaluations. Synthetic real-Git integration covers cold/hit/changed refresh, source addition/deletion, restored-mtime edits, duplicate reassessment, Demand composition, canary redaction and junction replacement. Focused regressions cover age/force/restart, partial recovery, cache memory, failed enumeration, collector budgets and invalid clocks. A point-in-time dependency audit reports zero vulnerabilities. Independent review findings were addressed with regression coverage; focused human review and required PR checks remain integration gates. Public schemas and dependencies are unchanged. The cache is ephemeral; persistent cache and owner workflows remain later work.

The proposed M2-S09 `Complete` and M2-S10 `Ready` transitions become authoritative only after this revision passes required pull-request CI and reviewed integration into protected `main`.

### M2-S10 owner inspection and correction workflow

[ADR-0027](../adr/0027-local-owner-correction-workflow.md) adds a first-party local JSON CLI and Community service for inspecting, declaring, correcting, disputing and rejecting Claims without an LLM. Exact bounded requests name an explicit Store configuration; mutations require the observed generation and canonical timestamp. Inspection exposes minimized structured assessment/provenance metadata. Correct/dispute/reject preserve original Claims and Evidence under distinct opaque historical identifiers and append immutable owner records. The effective Claim becomes disputed; declarations remain unobserved. Only validated atomic Store commitment and readback produce `saved`.

Authentic derivation composition preserves corrections, declarations and archival provenance across source refresh/removal and restart. Corrected capability/scope pairs suppress replacement inference, effective disputes conservatively become stale when observations change, and older derivations cannot roll state back. Core excludes historical targets linked by effective disputes from current task projection. Existing schema and byte bounds reject history exhaustion without pruning owner records. Conflicts and uncertain commits return fixed errors without automatic retry. Notes remain private inert data, omitted from normal CLI output. [Owner usage](../OWNER_WORKFLOW.md) documents the interface and limits.

Pinned Windows Node.js 24.20.0/npm 11.19.0 verification passed offline lockfile-enforced clean installation and the complete aggregate: 204 unit tests, every schema corpus, 35 integration tests and 17 behavioral evaluations. Real Store/CLI tests cover process restart, malformed/oversized/invalid UTF-8 input, canary-safe output, declarations and generation conflicts. Regressions cover repeated same-second corrections, immutable history after changed/removed sources, stale replay/timestamps, concurrent and interrupted/uncertain writes, recovery generations and history bounds. `FMU-E-005` verifies persisted owner precedence through DCP compilation; `FMU-E-011` verifies owner acknowledgment and failed-write preservation. Audit reports zero vulnerabilities. Independent review findings were addressed with regressions and final focused review found no blocking issue. Public schemas, dependencies and lockfile are unchanged; import/export/deletion, persistent cache, correction revocation and end-to-end source command composition remain later work.

The proposed M2-S10 `Complete` and M2-S11 `Ready` transitions become authoritative only after this revision passes required pull-request CI and reviewed integration into protected `main`.

### M2-S11 owner portability and verified local deletion

[ADR-0028](../adr/0028-owner-portability-and-verified-deletion.md) adds exact bounded local owner import/export/delete requests to the existing CLI. Import accepts only a valid public Portable Profile Export for the configured subject into an absent Store, then uses verified generation-zero commitment. Export reconstructs an allowlist projection, preserves typed assessments and correction/provenance graphs, replaces private prose and source locations, remaps opaque identifiers, scans for sensitive content and validates the public schema. An explicit existing destination receives an exclusively created, synchronized and reread artifact; existing copies are never overwritten and uncertain outcomes remain explicit.

Store writers and migration now coordinate with deletion through an exclusive mutation gate. Persistent content-free barriers block profile/cache recreation. Deletion removes only recognized Store generations/staging files, disposes subject-matching in-process incremental sessions, and composes adapter-owned cleanup with explicit all-local-adapter-cache scope. Bounded scans and post-removal verification constrain success. Source repositories, unknown files, final exports, independent backups and already returned objects remain outside deletion. Crashed gates and oversized corrupt files require documented owner maintenance; partial deletion can explicitly resume. Directory synchronization retains the documented Windows platform limit. The source-session registry is bounded to 128 live entries.

Pinned Windows Node.js 24.20.0/npm 11.19.0 verification passed offline lockfile-enforced clean installation and the complete aggregate: 216 unit tests, every schema corpus, 46 integration tests and 18 behavioral evaluations. `FMU-E-015` proves redacted portability and retained effective owner precedence after import. Fault injection covers staging/final readback, failed cleanup, barrier synchronization, interrupted/resumed deletion and in-flight disposal; real temporary-directory/subprocess tests cover cache races, junctions, temporary-parent aliases and untouched source/unrelated files. The point-in-time dependency audit reports zero vulnerabilities. Independent review findings were corrected and verified with regressions; focused human review and required PR checks remain integration gates. Public schemas, dependencies and lockfile are unchanged. No source deletion, private-data access, persistent source cache, network runtime, release or broader platform claim is introduced.

The proposed M2-S11 `Complete` and M2-S12 `Ready` transitions become authoritative only after this revision passes required pull-request CI and reviewed integration into protected `main`.

### M2-S12 bounded owner evidence and safe diagnostics

[ADR-0029](../adr/0029-owner-evidence-and-safe-diagnostics.md) adds read-only capability evidence and doctor requests to the local owner CLI. Evidence lookup selects one exact capability, bounds Claims/Evidence, reports truncation and preserves current versus historical correction semantics, including compatible self-targeted disputes. Nested metadata validates the existing Provider evidence contract. Hashes replace private identifiers, known limitation codes are allowlisted and unknown prose is omitted. Consumer Provider/MCP disclosure and public schemas stay unchanged.

Doctor probes actual module/runtime availability, selected Store schema/state and mutation gate, adapter-owned cache freshness/state/counts and optional DCP byte/token-upper-bound budgets and expiry. Each component reports its own state; `diagnosed` does not imply universal health. Store gates, cache files and directories remain untouched. Cache reads are bounded to 256 entries, 8 KiB per file and 2 MiB total, with regular-file/canonical checks and change detection. Missing installation dependencies produce fixed content-free failures. Client trust/hook registration, package integrity and other-process memory remain explicitly unchecked. No migration, repair, source reads, dependency change or network runtime is introduced.

Pinned Windows Node.js 24.20.0/npm 11.19.0 clean-install validation passed the full aggregate: 225 unit tests, all schema corpora, 56 integration tests and 19 behavioral evaluations. `FMU-E-015` covers profile/cache/task canaries and meaningful evidence/budget output. Synthetic CLI tests verify actual missing dependencies and read-only behavior; fault/limit tests cover stale/invalid/deleted/busy cache states, junctions, packet limits and abandoned Store gates. The point-in-time dependency audit reports zero vulnerabilities. Independent review findings were resolved with regressions. The owner explicitly authorizes integration after successful tests and PR CI, followed by the next M2 slice.

The proposed M2-S12 `Complete` and M2-S13 `Ready` transitions become authoritative only after required PR CI and authorized integration into protected `main`.

### M2-S13 complete local Community workflow

[ADR-0030](../adr/0030-local-community-workflow-composition.md) composes the authentic local configuration/collection/refresh/Store/owner boundaries into a first-party launcher. Explicit configuration binds source roots, identity, risk, refresh budgets, Store and current project. Store/source overlap is rejected. Bounded owner JSON commands perform verified correction-preserving refresh, inspection/correction, task context, diagnosis, export and deletion. Malformed commands preserve usable runtime state; accepted incomplete refresh preserves the Store and withholds aggregate context until a complete verified refresh. Source-cache reuse remains process-local.

A separate MCP mode serves the existing read surface from an asynchronous Store-backed Provider. Protected requests reload Store state, apply observation-age limits in a private copy, preserve effective correction precedence and compile through existing Demand/Core contracts. Consumers cannot collect, persist or supply source authority. Missing current-project metadata after restart uses explicit requested capabilities without inventing source metadata. The transport remains compatible with synchronous fixture providers; checked-in Codex fixture hooks and public schemas are unchanged. [Local usage](../LOCAL_COMMUNITY.md) covers setup through delivery and deletion.

Pinned Windows Node.js 24.20.0/npm 11.19.0 offline clean installation and the complete aggregate passed: 237 unit tests, all schema corpora, 58 integration tests and 19 behavioral evaluations. The actual CLI/MCP subprocess workflow uses two temporary real Git repositories and proves first collection, zero-collection cache reuse, rejection, changed-source refresh, restart, corrected context, doctor, exclusive redacted export and verified deletion with sources preserved. Negative tests cover invalid/oversized/redirected configuration, identity/project mismatch, directory overlap, incomplete refresh, malformed commands, deleted/future/stale state and consumer authority rejection. Independent review findings were corrected with regressions; the dependency audit reports zero vulnerabilities. No network/model service, private-data access, dependency change, public schema revision, release or quality measurement is introduced.

The proposed M2-S13 `Complete` and M2-S14 `Owner decision required` transitions become authoritative only after required PR CI and authorized integration into protected `main`. M2-S14 must freeze the sample and exact acceptable false-demonstrated/correction thresholds with the decision owner before M2-S15 measurement; the broad integration authorization does not supply those numeric decisions.

### M2-S14 researched evidence method and frozen protocol

The owner explicitly delegated research, method/sample/threshold selection and application after raising the limits of generated-code identification in modern assisted programming. [ADR-0031](../adr/0031-evidence-method-and-frozen-quality-protocol.md) records that decision authority. The [core evidence method](../EVIDENCE_METHOD.md) separates Git identity association, artifact indicators and human understanding using primary literature and official provenance documentation. It retains deterministic rules without AI detection or AI-use penalties and makes the existing two-observation heuristic and categorical-confidence limitations explicit.

The [frozen protocol](../evaluations/M2_QUALITY_PROTOCOL.md) and hashed manifest define 48 synthetic cases (24 families in TypeScript/Python), 52 primary Evidence records, ten assistance/committer invariance pairs, two fixed owner-correction/rejection exercises and pinned negative security controls. Every expected assertion must pass. Counts describe deterministic conformance, not measured human acceptance or population skill accuracy. Independent static review verified labels/arithmetic and identified recipe/control ambiguities that were resolved before freeze. No new-sample pipeline or quality measurement ran in this slice. The complete pinned Windows aggregate passed with 237 unit tests, all schema corpora, 58 integration tests and 19 behavioral evaluations; runtime, public schemas and dependencies are unchanged.

The M2-S14 `Complete` and M2-S15 `Ready` transitions become authoritative only after required PR CI and authorized integration of this frozen revision into `main`. M2-S15 must retain actual failed results and cannot tune these frozen inputs or thresholds after measurement.

### M2-S15 frozen quality measurement and machine transition

[ADR-0032](../adr/0032-reproducible-local-quality-measurement.md) adds a reproducible, bounded synthetic source-to-Store/Provider/owner evaluator with independent oracle scoring and adversarial scorer regressions. The integrated S14 freeze and all thirteen pinned input/control files are verified before measurement. No production algorithm, public schema, dependency, sample, oracle or acceptance threshold changed. The [measurement record and raw reports](../evaluations/M2_QUALITY_RESULTS.md) preserve actual results, including the nested-control failure and its isolated executor fix.

The final clean measurement at `747ad89e5a2491332b6a413813108af99590918f` passed: 48 accepted cases, zero correction-needed/rejected cases, zero false-demonstrated outcomes, 52/52 expected primary attribution records, ten expected unknown records, zero ceiling violations, ten invariances, two persisted owner exercises and 23 security controls without skips. Earlier repeated clean standalone reports were byte-identical. These are synthetic conformance results, not population accuracy or measured human understanding.

The pinned Windows offline clean install and full aggregate passed: 250 unit tests, all schema corpora, 59 integration tests and 19 behavioral evaluations. The point-in-time dependency audit reported zero vulnerabilities. Independent review found no remaining blocker. CI checks out the freeze history and preserves minimized failed integration reports; missing/skipped controls still fail closed.

The S15 `Complete` and S16 `Ready` transitions became authoritative after required PR CI and authorized integration into protected `main`. At the S15 stop point, the owner required continuation on another machine, so S16 could not start in that execution. The [historical machine handoff](../handoffs/M2_NEXT_MACHINE.md) preserves the decisions, reproducible setup, results, retained limitations, and exact audit scope that the later S16 task followed.

### M2-S16 M2 exit audit

The [M2 exit audit](../audits/M2_EXIT_AUDIT.md) independently reviews integrated revision `b95e3cb89b8a6543e3de8d966fe147abd0eaa395`, including the complete M2 deliverables, required evaluations, named security cases, exit criteria and cross-milestone gates. Focused review of the S15 squash commit found no production-runtime, public-schema or dependency change and no scoring/control defect. Current runtime, checks and workflow paths are identical to the measured implementation revision; the evaluator retains failed denominators and runs all frozen controls without inherited test-runner skip state.

A full clean Windows clone with Node.js 24.20.0 and npm 11.19.0 installed the locked graph without lifecycle scripts and passed formatting, lint, strict type checking, 250 unit tests, every schema corpus, 59 integrations and 19 behavioral evaluations with no failure or skip. A fresh frozen measurement passed 48/48 cases, 52/52 attribution records, ten expected unknown records, 10/10 invariances, 2/2 owner exercises and 23/23 controls. Dependency, license, tracked-file/symlink, credential-pattern, Markdown-link, local-authority and authenticated protected-main reviews found no unresolved critical/high issue or compatibility overclaim.

The only demonstrated gap was stale navigation and governance prose that still routed work to early M2/M1 state and described an ended integration exception. M2-S16 corrects those claims, adds no runtime behavior, contract, dependency, migration, release, platform support or source connector, and records all retained local limitations in the audit. No new `FMU-E-*` applies because this slice audits existing behavior rather than changing it.

The proposed M2 `Complete`, M3 `Ready`, M2-S16 `Complete`, and M3-S01 `Ready` transitions become authoritative only after this audit revision passes required pull-request CI and integrates into protected `main`.

## Verification coverage recorded at 84921df

The following execution narrative was moved unchanged from Engineering Section 7 on September 9, 2026. It describes its original implementation scope; consult the current engineering policy and tests for new work.

M2-S08 makes `FMU-E-009` and `FMU-E-010` executable at the private Demand producer boundary. Explicit candidate interpretations with different effective capabilities/relevance or operation risk yield one clarification; a valid choice resolves the pending object without another question. Equivalent interpretations, absent ambiguity and missing evidence proceed without a questionnaire. These tests exercise structured behavior, not model-authored questions or an owner/client UI.

M2-S10 exercises `FMU-E-005` through persisted/reloaded correction and Core DCP compilation: owner input controls behavior, archival knowledge is not projected as current, and original provenance stays privately inspectable. Its `FMU-E-011` evaluation verifies owner acknowledgment after persistence and prior-state retention after failed staging. Real subprocess tests cover the no-LLM CLI.

M2-S11 exercises `FMU-E-015` through redacted export, absent-only import and subsequent Core intersection: canaries stay out of artifacts/results and historical knowledge remains excluded after correction round trips. Unit fault injection covers persistence/readback/cleanup failures, deletion barriers and session disposal. Real filesystem/subprocess tests isolate Store and adapter roots, verify cache deletion and non-resurrection, reject junctions and preserve source/unrelated files. Full aggregate checks and focused human review remain integration gates.

M2-S12 extends `FMU-E-015` to owner evidence and doctor outputs, including sensitive private limitations, opaque identifiers, injected cache errors and DCP task content. Tests validate nested Provider metadata, historical/self-targeted correction behavior, byte/count bounds, real missing-dependency diagnostics, unchanged files/directories, cache expiry and junction rejection. Diagnostic success is checked independently from component health; no repair is performed.

M2-S13 verifies the local-utility exit gate through an actual CLI/MCP subprocess workflow over two temporary Git repositories: collection, unchanged cache reuse, persisted rejection, changed-source refresh, restart, corrected context, doctor, export and verified deletion. Unit tests independently exercise the Store-backed Provider and runtime failure boundaries, including no consumer source/write authority, malformed owner commands, partial refresh and Store/source overlap. Existing behavioral evaluations remain applicable; no new evaluation ID is invented for this composition gate.
